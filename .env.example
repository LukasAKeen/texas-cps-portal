# Vercel Postgres connection strings
# Paste these from your Vercel project's Storage tab

# Pooled connection (used by Prisma Client at runtime via adapter)
POSTGRES_PRISMA_URL=

# Direct connection (used by Prisma Migrate, bypasses PgBouncer)
POSTGRES_URL_NON_POOLING=
